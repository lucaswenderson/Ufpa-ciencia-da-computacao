#include <stdio.h>

int main(){
    int T, n;
    int p[] = { 1, 7, 9, 3 };
    scanf("%d", &T);
    while(T>0){
        scanf("%d", &n);
        printf("%d\n", p[n%4]);
        T--;
    }
    return 0;
}